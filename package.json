{
  "name": "nodejs-com-solid",
  "version": "1.0.0",
  "description": "- [] Deve ser possível se cadastrar;\r - [] Deve ser possível se autenticar;\r - [] Deve ser possível obter o perfil de um usuário logado;\r - [] Deve ser possível obter número de check-ins realizado pelo usuário logado;\r - [] Deve ser possível o usuário obter seu histórico de check-ins;\r - [] Deve ser possível o usuário buscar academias próximas;\r - [] Deve ser possível o usuário buscar academias pelo nome;\r - [] Deve ser possível o usuário realizar check-in em uma academia;\r - [] Deve ser possível validar o check-in de um usuário;\r - [] Deve ser possível cadastrar uma academia;",
  "main": "index.js",
  "scripts": {
    "start": "node build/server.js",
    "dev": "tsx watch src/server.ts",
    "build": "tsup src --out-dir build",
    "tscInit": "npx tsc --init",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.10.7",
    "tsup": "^8.0.1",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "fastify": "^4.25.2"
  }
}
